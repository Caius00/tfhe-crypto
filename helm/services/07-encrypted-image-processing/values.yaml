namespace: 07-encrypted-image-processing
replicaCount: 1

image:
  repository: ghcr.io/caius00/encrypted-image-processing
  tag: "0.1.3"
  pullPolicy: IfNotPresent

service:
  port: 8080

resources:
  requests:
    cpu: 500m
    memory: 512Mi
  limits:
    cpu: "2"
    memory: 1536Mi

route:
  path: /image-processing
  gateway:
    name: traefik-gateway
    namespace: traefik
    sectionName: web
