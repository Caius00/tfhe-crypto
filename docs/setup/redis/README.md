# Redis

**URL:** `redis://localhost:6379`

| Variable | Wert |
|----------|------|
| Host | `localhost` |
| Port | `6379` |

**Starten**
```bash
docker compose up -d
```

**Stoppen**
```bash
docker compose down
```

**Löschen** (inkl. Daten)
```bash
docker compose down -v
```
