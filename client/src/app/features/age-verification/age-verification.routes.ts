import { Routes } from '@angular/router';
import { AgeVerificationComponent } from './age-verification.component';

export const AGE_VERIFICATION_ROUTES: Routes = [
  { path: '', component: AgeVerificationComponent },
];
