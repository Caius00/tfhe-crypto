import { Routes } from '@angular/router';
import { KeyValueStoreComponent } from './key-value-store.component';

export const KEY_VALUE_STORE_ROUTES: Routes = [
  { path: '', component: KeyValueStoreComponent },
];
